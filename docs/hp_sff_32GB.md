# Explanation

This machine will act as not only the DNS server via Pihole, but also as the git (forgejo) and Nextcloud server. 

Nextcloud will not only serve as my own personal cloud, but also as a backup plan for the repos that are going to be in the git server.

# Setup

I will also use Proxmox for this machine, one VM for the Pihole and one VM running talos for the Nextcloud and the git server.

# Deadline

I'd like to finish this ASAP, but it really depends on when I am done with my machine learning exam, since the two HP machines are running two services I need for that.

End of September at the latest.